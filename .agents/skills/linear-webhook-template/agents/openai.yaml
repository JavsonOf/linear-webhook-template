interface:
  display_name: "Linear Webhook Template"
  short_description: "Repo-specific patterns and workflows for linear-webhook-template"
  default_prompt: "Use the linear-webhook-template repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true